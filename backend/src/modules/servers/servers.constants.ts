/**
 * Filename of the JSON manifest written alongside server files when a server
 * is exported, and required at the root of any archive to be re-imported
 */
export const EXPORT_MANIFEST_FILE = "kubek-manifest.json";

/**
 * Version of the manifest schema
 */
export const EXPORT_MANIFEST_VERSION = 1;

/**
 * Blueprint id for custom jar
 */
export const CUSTOM_BLUEPRINT_ID = "com.kubek.custom";
